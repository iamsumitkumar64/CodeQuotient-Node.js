<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIGN-UP</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #6e6d6d;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            margin: 0;
        }

        h1 {
            font-size: 4vmin;
            margin-bottom: 2vmin;
        }

        form {
            color: white;
            background-color: #000000;
            padding: 3%;
            font-size: 2vmax;
            font-weight: 900;
            border-radius: 1vmin;
            box-shadow: 0 0 10vmin rgba(0, 0, 0, 0.1);
            width: 50vmin;
        }

        ::placeholder {
            color: black;
        }

        .text {
            font-size: 2vmin;
            margin-bottom: 1vmin;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"] {
            width: 95%;
            padding: 1.5vmin;
            margin-bottom: 2vmin;
            border: 1px solid #ccc;
            border-radius: 0.5vmin;
            font-size: 2vmin;
        }

        input[type="text"]:focus,
        input[type="email"]:focus,
        input[type="password"]:focus {
            border-color: #007BFF;
            outline: none;
        }

        button,
        a {
            padding: 1.5vmin;
            background-color: #007BFF;
            color: white;
            font-weight: 900;
            border: none;
            border-radius: 0.5vmin;
            font-size: 2vmin;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        a {
            text-decoration: none;
        }

        button:hover,
        a:hover {
            background-color: #003670;
        }
    </style>
</head>

<body>
    <h1>SIGN-UP PAGE FOR PORTAL</h1>
    <form method="post" action="/auth/signup">
        <label for="name" class="text">Enter Username :</label>
        <input type="text" placeholder="Enter Username" name="name" required>

        <label for="name" class="text">Enter Email :</label>
        <input type="email" placeholder="Enter Email" name="email" required>

        <label for="pass" class="text">Enter Password :</label>
        <input type="password" name="password" placeholder="e.g=@H21" required>

        <button type="submit">Submit</button>
        <a href="http://localhost:8000/auth/login" class="button">Already Have An Account</a>
    </form>
</body>

</html>